# Brand assets — internal note

The marks in `public/brand/` are IEP Partners' own, used under the DataIsData engagement.
Do not modify, recolor, or stretch them.

This note lives in `docs/` rather than in `public/`, because everything in `public/` is
copied verbatim into the published site. A note naming the engagement would then be one
URL away from anyone in a meeting, which is exactly what CLAUDE.md §1.1 exists to prevent.

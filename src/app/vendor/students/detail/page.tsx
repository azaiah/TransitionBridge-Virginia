'use client';

import { RecordRoute } from '@/components/record/RecordRoute';

/** The secure transition record, as this portal sees it. See src/lib/access.ts. */
export default function TransitionRecordPage() {
  return <RecordRoute />;
}
